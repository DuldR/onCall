<%= form_with(model: swap) do |form| %>
  <% if swap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(swap.errors.count, "error") %> prohibited this swap from being saved:</h2>

      <ul>
        <% swap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
